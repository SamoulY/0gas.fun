# Test Suite

This folder contains repository-level tests for backend flow logic and API smoke checks.

## Run

From `backend/`:

```bash
npm test
```

## Temporary Artifacts

All test temporary files must be written under repository `tmp/`.
Current smoke test output file:

- `tmp/test-human-data.jsonl`
